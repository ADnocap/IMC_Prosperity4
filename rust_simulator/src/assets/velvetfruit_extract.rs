//! VELVETFRUIT_EXTRACT — Round 3 product. Calibration in progress.
//!
//! STUB FILE — not yet registered in mod.rs / REGISTRY. Exists so the
//! dashboard's Calibration tab lists this asset in its dropdown.
//! Replace with a real `AssetSim` implementation once params.json is finalized.

#[allow(dead_code)]
pub const SYMBOL: &str = "VELVETFRUIT_EXTRACT";
